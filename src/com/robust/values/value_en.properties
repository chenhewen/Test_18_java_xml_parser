# Comment to tell you this is the english file
cmd.diff.use=diff srcDir outputDir modleFile
cmd.diff.tip=create a directory of files which compare srcDir to modleFile to produce a distinct outputDir
cmd.help.use=help
cmd.help.tip=list all commands
cmd.fill.use="fill srcDir destDir"
cmd.fill.tip="fill destDir with srcDir, if one item key already exits in a single file, it will overwrite it, "